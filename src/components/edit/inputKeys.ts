export const taskTitleInputKey = (taskId: string) => `task-${taskId}-title`;
export const taskNotesInputKey = (taskId: string) => `task-${taskId}-notes`;
export const taskDatePickerInputKey = 'desk-task-date-picker-date';
export const projectHeadingTitleInputKey = (projectHeadingId: string) => `project-heading-${projectHeadingId}-title`;
export const projectTitleInputKey = (projectId: string) => `project-${projectId}-title`;
export const areaTitleInputKey = (areaId: string) => `area-${areaId}-title`;
